// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://dan1294x.github.io/NonDesignerBook/', // URL final
    base: '/NonDesignerBook/', 
});
